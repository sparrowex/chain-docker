[parity]

[network]
port = 30300

[websockets]
port = 8546

# uncomment the following to provide a widely accessible RPC
# [rpc]
# disable = false
# port = 8545
# interface = "all"
# cors = ["*"]
# apis = ["web3", "eth", "pubsub", "net", "parity", "parity_pubsub", "traces", "rpc", "shh", "shh_pubsub"]
